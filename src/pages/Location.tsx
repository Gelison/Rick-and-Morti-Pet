import { useState } from 'react';
import { Character } from '../API/types';

export function Location() {
  const [films, setFilms] = useState<Character[]>([]);

  return <div className=''></div>;
}
